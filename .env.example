# Swingcam Environment Configuration
# Copy this file to .env and adjust as needed

# Data directory for storing uploads and clips
# For Railway: set to /data (where Volume is mounted)
# For local dev: set to ./data
DATA_DIR=/data

# Server port (Railway sets this automatically)
PORT=8000
